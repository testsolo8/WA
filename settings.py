from pprint import pprint

BROWSER_OPTIONS = {
    "geolocation": {"latitude": 48.8, "longitude": 2.3},
    "permissions": ["geolocation"],
}

SECURE_OPTIONS = {"security.enterprise_roots.enabled": True}
